import React from "react";
import ReactDOM from "react-dom";
import App from "./App";

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById("root")
);

// to make a new react app with TypeScript
//
// yarn create react-app todo-react-typescript --typescript
